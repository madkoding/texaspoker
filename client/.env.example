# Override the WebSocket URL used by the client.
# Leave empty/unset to auto-detect (connects to ws://localhost:3001/ws
# when running the Vite dev server, otherwise to the same origin).
# VITE_WS_URL=ws://localhost:3001/ws
